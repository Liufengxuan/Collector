03指令计算正确返回长度---------------------------------------------------

2(站号和操作码)
+
1(返回的字节数)
+
int a=BitConvert.Toint32(tx[tx.length-3],tx[tx.length-4])*2
+
2(crc校验)


06指令计算正确返回长度---------------------------------------------------

固定 tx.length

10指令计算正确返回长度---------------------------------------------------
固定 8


